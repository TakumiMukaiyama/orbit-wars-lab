# Phase 1c Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 1b から判明した 7 つのロジックギャップ (+ 新規 3 つ) のうち高優先度 5 つを実装し、TrueSkill スコアを改善する

**Architecture:**
- `targeting.py` に `planned_commitments`, `classify_defense`, orbital rival ETA 修正, end-game フィルターを追加
- `agent.py` に state machine (domination mode) と planned dict 管理を追加
- 各フィーチャー実装後に smoke test → gauntlet 計測を挟む

**Tech Stack:** Python 3.13, uv, pytest

---

## ファイルマップ

| ファイル | 変更内容 |
|---------|---------|
| `src/targeting.py` | `ships_budget` 拡張, `classify_defense` 追加, `compute_rival_eta` に orbital 対応, `enumerate_candidates` に planned/endgame 引数追加 |
| `src/agent.py` | `planned` dict 管理, domination mode 計算, doomed 判定でエバキュエーション発射 |
| `tests/test_targeting.py` | 各フィーチャーのテスト追加 |

---

## Task 1: In-flight Fleet Tracking - ships_budget 拡張 (P1)

**概要:** `ships_budget` に `my_eta` と `already_sent` を追加し、到着時点のガリソン増分と既送艦を差し引いた正確な必要艦数を返す。これで連射問題と ETA 過小見積もりを同時修正する。

**Files:**
- Modify: `agents/mine/planet_intercept/src/targeting.py:26-28`
- Test: `agents/mine/planet_intercept/tests/test_targeting.py`

- [ ] **Step 1: 失敗テストを書く**

`tests/test_targeting.py` の `TestShipsBudget` クラスに以下を追加:

```python
def test_eta_production_included(self):
    t = P(0, 1, 0, 0, ships=10, prod=2)
    # my_eta=5 -> garrison_at_arrival = 10 + 2*5 = 20 -> budget = 21
    assert ships_budget(t, my_eta=5.0) == 21

def test_already_sent_subtracted(self):
    t = P(0, 1, 0, 0, ships=10, prod=2)
    # garrison=20, already_sent=8 -> 20 - 8 + 1 = 13
    assert ships_budget(t, my_eta=5.0, already_sent=8) == 13

def test_already_sent_covers_all_returns_one(self):
    t = P(0, 1, 0, 0, ships=10, prod=0)
    # garrison=10, already_sent=15 -> max(1, 10 - 15 + 1) = 1
    assert ships_budget(t, my_eta=0.0, already_sent=15) == 1

def test_backward_compat_no_args(self):
    t = P(0, 1, 0, 0, ships=10)
    assert ships_budget(t) == 11
```

- [ ] **Step 2: 失敗確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py::TestShipsBudget -v
```

期待: `test_eta_production_included`, `test_already_sent_subtracted`, `test_already_sent_covers_all_returns_one` が FAIL

- [ ] **Step 3: targeting.py の ships_budget を修正**

`targeting.py` の `ships_budget` 関数を以下に置き換え:

```python
def ships_budget(target: Planet, my_eta: float = 0.0, already_sent: int = 0) -> int:
    """占領に必要な最小艦船量。

    到着時点のガリソン (駐留 + production * ETA) から既送艦を差し引く。
    """
    garrison_at_arrival = target.ships + int(target.production * my_eta)
    return max(1, garrison_at_arrival - already_sent + 1)
```

- [ ] **Step 4: テスト通過確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py::TestShipsBudget -v
```

期待: 全 PASS

- [ ] **Step 5: コミット**

```bash
git add agents/mine/planet_intercept/src/targeting.py agents/mine/planet_intercept/tests/test_targeting.py
git commit -m "feat(targeting): extend ships_budget with my_eta and already_sent"
```

---

## Task 2: In-flight Fleet Tracking - planned dict と enumerate_candidates 接続 (P1 続き)

**概要:** `agent.py` にターンごとの `planned: dict[int, int]` を管理し、`enumerate_candidates` が `already_sent` として使えるよう引数を追加する。

**Files:**
- Modify: `agents/mine/planet_intercept/src/targeting.py:87-133` (enumerate_candidates)
- Modify: `agents/mine/planet_intercept/src/agent.py`
- Test: `agents/mine/planet_intercept/tests/test_targeting.py`

- [ ] **Step 1: 失敗テストを書く**

`tests/test_targeting.py` の `TestEnumerateCandidates` に追加:

```python
def test_planned_reduces_ships_needed(self):
    mine = P(0, 0, 0, 0, ships=50)
    target = P(1, 1, 10, 0, ships=10, prod=0)
    planets = [mine, target]
    # planned なし: ships_needed = 10 + 0*eta + 1 = 11 相当
    cands_no_plan = enumerate_candidates(mine, planets, fleets=[], player=0, planned={})
    # planned あり: target に既送 8 ships -> ships_needed 減少
    cands_planned = enumerate_candidates(mine, planets, fleets=[], player=0, planned={1: 8})
    ships_no = next(c[1] for c in cands_no_plan if c[0].id == 1)
    ships_pl = next(c[1] for c in cands_planned if c[0].id == 1)
    assert ships_pl < ships_no

def test_planned_fully_covered_returns_one(self):
    mine = P(0, 0, 0, 0, ships=50)
    target = P(1, 1, 10, 0, ships=5, prod=0)
    planets = [mine, target]
    # already_sent=100 で max(1,...) = 1
    cands = enumerate_candidates(mine, planets, fleets=[], player=0, planned={1: 100})
    ships_needed = next(c[1] for c in cands if c[0].id == 1)
    assert ships_needed == 1
```

- [ ] **Step 2: 失敗確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py::TestEnumerateCandidates -v
```

期待: 追加した 2 テストが FAIL (planned 引数が存在しない)

- [ ] **Step 3: enumerate_candidates に planned 引数を追加**

`targeting.py` の `enumerate_candidates` 関数シグネチャと本体を修正:

```python
def enumerate_candidates(
    my_planet: Planet,
    all_planets,
    fleets,
    player: int,
    top_n: int = 16,
    angular_velocity: float = 0.0,
    planned: dict | None = None,
):
    """自分以外が所有する惑星をインターセプト位置で距離昇順ソートし上位 top_n 件を返す。

    返り値: list[(target, ships_needed, angle, value)]
    """
    from .utils import CENTER

    if planned is None:
        planned = {}

    targets = [p for p in all_planets if p.owner != player and p.id != my_planet.id]

    def sort_key(t):
        r = math.hypot(t.x - CENTER, t.y - CENTER)
        is_orbital = angular_velocity != 0.0 and (r + t.radius < 50)
        cur_dist = distance(my_planet, t)
        return (1 if is_orbital else 0, cur_dist)

    targets.sort(key=sort_key)
    targets = targets[:top_n]

    out = []
    for t in targets:
        r = math.hypot(t.x - CENTER, t.y - CENTER)
        is_orbital = angular_velocity != 0.0 and (r + t.radius < 50)
        if is_orbital:
            # intercept_pos は ships_needed に依存するが、1 回近似で十分
            ships_approx = ships_budget(t)
            ix, iy, my_eta = intercept_pos(
                my_planet.x, my_planet.y, ships_approx, t, angular_velocity
            )
        else:
            ix, iy = t.x, t.y
            ships_approx = ships_budget(t)
            my_eta = route_eta(my_planet.x, my_planet.y, ix, iy, ships_approx)
        if segment_hits_sun(my_planet.x, my_planet.y, ix, iy):
            continue
        angle, _ = route_angle_and_distance(my_planet.x, my_planet.y, ix, iy)
        already_sent = planned.get(t.id, 0)
        ships_needed = ships_budget(t, my_eta=my_eta, already_sent=already_sent)
        rival_eta = compute_rival_eta(t, player, fleets, all_planets)
        value = target_value(
            my_planet, ix, iy, t.production, rival_eta, ships_needed, my_eta, target_owner=t.owner
        )
        out.append((t, ships_needed, angle, value))
    return out
```

- [ ] **Step 4: テスト通過確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py -v
```

期待: 全 PASS

- [ ] **Step 5: agent.py に planned dict を組み込む**

`agent.py` を以下に全面置き換え:

```python
"""Phase 1c エージェント: In-flight tracking + Doomed evacuation + State machine."""

from .targeting import (
    classify_defense,
    enumerate_candidates,
    enumerate_intercept_candidates,
    select_move,
)
from .utils import angle_to, parse_obs


def agent(obs):
    player, planets, fleets, angular_velocity = parse_obs(obs)

    my_planets = [p for p in planets if p.owner == player]
    if not my_planets:
        return []

    n = len(my_planets)

    # planned[planet_id] = このターンに既に送った ships 合計
    planned: dict[int, int] = {}

    moves = []
    for mine in my_planets:
        status, reserve = classify_defense(mine, fleets, player)

        if status == "doomed" and n > 1:
            # 最も近い自惑星に全艦退避
            allies = [p for p in planets if p.owner == player and p.id != mine.id]
            if allies:
                nearest_ally = min(allies, key=lambda p: (p.x - mine.x) ** 2 + (p.y - mine.y) ** 2)
                import math
                evac_angle = math.atan2(nearest_ally.y - mine.y, nearest_ally.x - mine.x)
                evac_ships = mine.ships
                if evac_ships > 0:
                    moves.append([mine.id, evac_angle, evac_ships])
            continue

        attack_cands = enumerate_candidates(
            mine,
            planets,
            fleets,
            player,
            angular_velocity=angular_velocity,
            planned=planned,
        )
        intercept_cands = enumerate_intercept_candidates(
            mine,
            planets,
            fleets,
            player,
            angular_velocity=angular_velocity,
        )
        all_cands = attack_cands + intercept_cands
        picked = select_move(mine, all_cands, reserve=reserve, my_planet_count=n)
        if picked is None:
            continue
        angle, ships = picked
        # 送出した ships を planned に記録
        # (発射ターゲットを angle から逆引きするより、候補を持ち回る方が正確だが
        #  select_move が target を返さないため angle 照合で近似する)
        for target, ships_needed, cand_angle, _ in all_cands:
            if abs(cand_angle - angle) < 1e-9 and ships_needed == ships:
                planned[target.id] = planned.get(target.id, 0) + ships
                break
        moves.append([mine.id, angle, ships])

    return moves
```

- [ ] **Step 6: 全テスト通過確認**

```bash
cd agents/mine/planet_intercept && uv run pytest -v
```

期待: 全 PASS (agent.py は classify_defense が未定義のため Task 3 まで FAIL 可)

- [ ] **Step 7: コミット**

```bash
git add agents/mine/planet_intercept/src/targeting.py agents/mine/planet_intercept/src/agent.py agents/mine/planet_intercept/tests/test_targeting.py
git commit -m "feat(agent): in-flight fleet tracking with planned commitments"
```

---

## Task 3: Doomed Detection + Evacuation (P2)

**概要:** `estimate_reserve` を `classify_defense` に置き換え、惑星を "safe" / "threatened" / "doomed" に分類する。doomed 判定時は艦船を退避させる (Task 2 の agent.py で使用済み)。

**Files:**
- Modify: `agents/mine/planet_intercept/src/targeting.py`
- Test: `agents/mine/planet_intercept/tests/test_targeting.py`

- [ ] **Step 1: 失敗テストを書く**

`tests/test_targeting.py` に新クラスを追加:

```python
from src.targeting import classify_defense

class TestClassifyDefense:
    def test_no_incoming_is_safe(self):
        mine = P(0, 0, 50, 50, ships=50)
        status, reserve = classify_defense(mine, fleets=[], player=0)
        assert status == "safe"
        assert reserve == 0

    def test_threatened_when_outgunned_but_can_hold(self):
        mine = P(0, 0, 50, 50, ships=50)
        enemy = F(1, 1, 30, 50, angle=0.0, from_id=99, ships=30)
        status, reserve = classify_defense(mine, [enemy], player=0)
        # mine.ships=50 >= incoming=30 -> threatened
        assert status == "threatened"
        assert reserve == 30

    def test_doomed_when_cannot_hold(self):
        mine = P(0, 0, 50, 50, ships=10)
        enemy = F(1, 1, 30, 50, angle=0.0, from_id=99, ships=30)
        status, reserve = classify_defense(mine, [enemy], player=0)
        # mine.ships=10 < incoming=30 -> doomed
        assert status == "doomed"
        assert reserve == 30

    def test_own_fleet_not_counted(self):
        mine = P(0, 0, 50, 50, ships=10)
        own = F(1, 0, 30, 50, angle=0.0, from_id=99, ships=30)
        status, reserve = classify_defense(mine, [own], player=0)
        assert status == "safe"

    def test_sideways_fleet_ignored(self):
        mine = P(0, 0, 50, 50, ships=10)
        sideways = F(1, 1, 20, 20, angle=0.0, from_id=99, ships=30)
        status, reserve = classify_defense(mine, [sideways], player=0)
        assert status == "safe"
```

- [ ] **Step 2: 失敗確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py::TestClassifyDefense -v
```

期待: ImportError または全 FAIL

- [ ] **Step 3: classify_defense を targeting.py に追加**

`targeting.py` の `estimate_reserve` 関数の直後に追加:

```python
def classify_defense(
    mine: Planet, fleets, player: int
) -> tuple[str, int]:
    """mine の防衛状況を ("safe"|"threatened"|"doomed", incoming_ships) で返す。

    "doomed": 守れない (mine.ships < incoming)
    "threatened": 守れる (mine.ships >= incoming > 0)
    "safe": 敵フリートなし
    """
    incoming = 0
    for f in fleets:
        if f.owner == player:
            continue
        if fleet_heading_to(f, mine):
            incoming += f.ships
    if incoming == 0:
        return "safe", 0
    if mine.ships >= incoming:
        return "threatened", incoming
    return "doomed", incoming
```

- [ ] **Step 4: テスト通過確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py::TestClassifyDefense -v
```

期待: 全 PASS

- [ ] **Step 5: 全テスト通過確認**

```bash
cd agents/mine/planet_intercept && uv run pytest -v
```

期待: 全 PASS

- [ ] **Step 6: コミット**

```bash
git add agents/mine/planet_intercept/src/targeting.py agents/mine/planet_intercept/tests/test_targeting.py
git commit -m "feat(targeting): add classify_defense for doomed/threatened/safe detection"
```

---

## Task 4: State Machine - domination mode (P3)

**概要:** `agent.py` の先頭でゲーム全体の domination スコアを計算し、"behind" / "neutral" / "ahead" モードを決定する。モードを `enumerate_candidates` と `select_move` に渡し、behind では HOLD_HORIZON を縮小して無謀な攻撃を抑制する。

**Files:**
- Modify: `agents/mine/planet_intercept/src/targeting.py` (HOLD_HORIZON を動的に)
- Modify: `agents/mine/planet_intercept/src/agent.py`
- Test: `agents/mine/planet_intercept/tests/test_targeting.py`

- [ ] **Step 1: 失敗テストを書く**

`tests/test_targeting.py` に追加:

```python
from src.targeting import compute_domination, BEHIND_THRESHOLD, AHEAD_THRESHOLD

class TestComputeDomination:
    def test_equal_returns_zero(self):
        dom = compute_domination(my_total=100, enemy_total=100)
        assert dom == pytest.approx(0.0)

    def test_all_mine_returns_one(self):
        dom = compute_domination(my_total=100, enemy_total=0)
        assert dom == pytest.approx(1.0)

    def test_all_enemy_returns_minus_one(self):
        dom = compute_domination(my_total=0, enemy_total=100)
        assert dom == pytest.approx(-1.0)

    def test_thresholds_exist(self):
        assert BEHIND_THRESHOLD < 0
        assert AHEAD_THRESHOLD > 0

class TestTargetValueWithMode:
    def test_behind_mode_reduces_neutral_value(self):
        mine = P(0, 0, 0, 0, ships=50)
        v_neutral = target_value(
            mine, 20.0, 0.0, 3, math.inf, ships_to_send=7, my_eta=5.0,
            target_owner=NEUTRAL_OWNER, mode="neutral",
        )
        v_behind = target_value(
            mine, 20.0, 0.0, 3, math.inf, ships_to_send=7, my_eta=5.0,
            target_owner=NEUTRAL_OWNER, mode="behind",
        )
        assert v_behind < v_neutral
```

- [ ] **Step 2: 失敗確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py::TestComputeDomination tests/test_targeting.py::TestTargetValueWithMode -v
```

期待: ImportError または全 FAIL

- [ ] **Step 3: targeting.py に compute_domination と mode 対応を追加**

`targeting.py` の定数セクション (`HOLD_HORIZON` 等) を修正し、関数を追加:

```python
HOLD_HORIZON = 10.0
HOLD_HORIZON_BEHIND = 4.0   # behind モードでの中立惑星時間軸短縮
THREAT_MARGIN = 0.0
TRAVEL_PENALTY = 0.0

BEHIND_THRESHOLD = -0.3
AHEAD_THRESHOLD = 0.3
```

`compute_domination` を `compute_rival_eta` の直後に追加:

```python
def compute_domination(my_total: int, enemy_total: int) -> float:
    """domination スコア: (my - enemy) / (my + enemy)。範囲 [-1, 1]。"""
    total = my_total + enemy_total
    if total == 0:
        return 0.0
    return (my_total - enemy_total) / total
```

`target_value` のシグネチャに `mode: str = "neutral"` を追加し、HOLD_HORIZON 分岐を修正:

```python
def target_value(
    mine: Planet,
    target_x: float,
    target_y: float,
    production: int,
    rival_eta: float,
    ships_to_send: int,
    my_eta: float,
    target_owner: int = NEUTRAL_OWNER,
    mode: str = "neutral",
) -> float:
    """占領価値。

    中立 (target_owner == NEUTRAL_OWNER):
        rival 未脅威 -> production * horizon - ships - my_eta * TRAVEL_PENALTY
        rival 脅威あり -> production * max(0, rival_eta - my_eta) - ships
    敵惑星 (target_owner != NEUTRAL_OWNER):
        production * max(0, rival_eta - my_eta) - ships
    """
    horizon = HOLD_HORIZON_BEHIND if mode == "behind" else HOLD_HORIZON
    threat = math.isfinite(rival_eta) and (rival_eta - my_eta) <= THREAT_MARGIN
    if target_owner == NEUTRAL_OWNER:
        if not threat:
            return production * horizon - ships_to_send - my_eta * TRAVEL_PENALTY
        return production * max(0.0, rival_eta - my_eta) - ships_to_send
    gain = production * max(0.0, rival_eta - my_eta)
    return gain - ships_to_send
```

`enumerate_candidates` に `mode: str = "neutral"` 引数を追加し、`target_value` 呼び出しに渡す:

```python
def enumerate_candidates(
    my_planet: Planet,
    all_planets,
    fleets,
    player: int,
    top_n: int = 16,
    angular_velocity: float = 0.0,
    planned: dict | None = None,
    mode: str = "neutral",
):
    # ... (既存ロジック) ...
    # target_value 呼び出し部分のみ変更:
    value = target_value(
        my_planet, ix, iy, t.production, rival_eta, ships_needed, my_eta,
        target_owner=t.owner, mode=mode,
    )
```

- [ ] **Step 4: テスト通過確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py -v
```

期待: 全 PASS

- [ ] **Step 5: agent.py に domination 計算を追加**

`agent.py` の `agent()` 関数先頭に追加 (my_planets 判定の直後):

```python
from .targeting import (
    AHEAD_THRESHOLD,
    BEHIND_THRESHOLD,
    classify_defense,
    compute_domination,
    enumerate_candidates,
    enumerate_intercept_candidates,
    select_move,
)

def agent(obs):
    player, planets, fleets, angular_velocity = parse_obs(obs)

    my_planets = [p for p in planets if p.owner == player]
    if not my_planets:
        return []

    n = len(my_planets)

    # domination mode
    my_total = (
        sum(p.ships for p in my_planets)
        + sum(f.ships for f in fleets if f.owner == player)
    )
    enemy_total = (
        sum(p.ships for p in planets if p.owner not in (player, -1))
        + sum(f.ships for f in fleets if f.owner not in (player, -1))
    )
    dom = compute_domination(my_total, enemy_total)
    if dom < BEHIND_THRESHOLD:
        mode = "behind"
    elif dom > AHEAD_THRESHOLD:
        mode = "ahead"
    else:
        mode = "neutral"

    planned: dict[int, int] = {}

    moves = []
    for mine in my_planets:
        status, reserve = classify_defense(mine, fleets, player)

        if status == "doomed" and n > 1:
            allies = [p for p in planets if p.owner == player and p.id != mine.id]
            if allies:
                import math
                nearest_ally = min(allies, key=lambda p: (p.x - mine.x) ** 2 + (p.y - mine.y) ** 2)
                evac_angle = math.atan2(nearest_ally.y - mine.y, nearest_ally.x - mine.x)
                evac_ships = mine.ships
                if evac_ships > 0:
                    moves.append([mine.id, evac_angle, evac_ships])
            continue

        attack_cands = enumerate_candidates(
            mine,
            planets,
            fleets,
            player,
            angular_velocity=angular_velocity,
            planned=planned,
            mode=mode,
        )
        intercept_cands = enumerate_intercept_candidates(
            mine,
            planets,
            fleets,
            player,
            angular_velocity=angular_velocity,
        )
        all_cands = attack_cands + intercept_cands
        picked = select_move(mine, all_cands, reserve=reserve, my_planet_count=n)
        if picked is None:
            continue
        angle, ships = picked
        for target, ships_needed, cand_angle, _ in all_cands:
            if abs(cand_angle - angle) < 1e-9 and ships_needed == ships:
                planned[target.id] = planned.get(target.id, 0) + ships
                break
        moves.append([mine.id, angle, ships])

    return moves
```

- [ ] **Step 6: 全テスト通過確認**

```bash
cd agents/mine/planet_intercept && uv run pytest -v
```

期待: 全 PASS

- [ ] **Step 7: コミット**

```bash
git add agents/mine/planet_intercept/src/targeting.py agents/mine/planet_intercept/src/agent.py agents/mine/planet_intercept/tests/test_targeting.py
git commit -m "feat(agent): state machine with domination-based mode switching"
```

---

## Task 5: Orbital rival ETA 修正 (新規ギャップ 1)

**概要:** `compute_rival_eta` が軌道惑星に対して現在位置で ETA 計算するバグを修正。軌道惑星のとき `angular_velocity` を渡して intercept_pos ベースで rival ETA を推定する。

**Files:**
- Modify: `agents/mine/planet_intercept/src/targeting.py`
- Test: `agents/mine/planet_intercept/tests/test_targeting.py`

- [ ] **Step 1: 失敗テストを書く**

`tests/test_targeting.py` に追加:

```python
class TestComputeRivalEtaOrbital:
    def test_orbital_rival_eta_differs_from_static(self):
        """angular_velocity があるとき rival ETA が静止計算と異なること。"""
        target = P(1, -1, 30, 50, ships=5)  # orbital: radius from center < 50
        rival_planet = P(2, 1, 70, 50, ships=15)
        planets = [P(0, 0, 10, 50, ships=10), rival_planet, target]
        eta_static = compute_rival_eta(target, my_player=0, fleets=[], planets=planets,
                                        angular_velocity=0.0)
        eta_orbital = compute_rival_eta(target, my_player=0, fleets=[], planets=planets,
                                         angular_velocity=0.03)
        # 軌道惑星なので両者は異なるはず (惑星が動くため目標位置が変わる)
        assert eta_static != pytest.approx(eta_orbital, rel=0.01)
```

- [ ] **Step 2: 失敗確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py::TestComputeRivalEtaOrbital -v
```

期待: FAIL (angular_velocity 引数が未定義)

- [ ] **Step 3: compute_rival_eta_per_player と compute_rival_eta に angular_velocity を追加**

`targeting.py` の `compute_rival_eta_per_player` を修正:

```python
def compute_rival_eta_per_player(
    target: Planet, my_player: int, fleets, planets, angular_velocity: float = 0.0
) -> dict:
    """プレイヤー別の最速 ETA dict を返す。自分と中立は含まない。"""
    from .utils import CENTER

    r_target = math.hypot(target.x - CENTER, target.y - CENTER)
    is_orbital = angular_velocity != 0.0 and (r_target + target.radius < 50)

    per: dict = {}
    for f in fleets:
        if f.owner == my_player:
            continue
        if is_orbital:
            result = intercept_pos(f.x, f.y, max(1, f.ships), target, angular_velocity)
            e = result[2]
        else:
            e = route_eta(f.x, f.y, target.x, target.y, max(1, f.ships))
        if e < per.get(f.owner, math.inf):
            per[f.owner] = e

    for p in planets:
        if p.owner == my_player or p.owner == NEUTRAL_OWNER:
            continue
        if p.id == target.id:
            continue
        ships = max(1, p.ships)
        if is_orbital:
            result = intercept_pos(p.x, p.y, ships, target, angular_velocity)
            e = result[2]
        else:
            e = route_eta(p.x, p.y, target.x, target.y, ships)
        if e < per.get(p.owner, math.inf):
            per[p.owner] = e

    return per


def compute_rival_eta(
    target: Planet, my_player: int, fleets, planets, angular_velocity: float = 0.0
) -> float:
    """自分以外のプレイヤーがターゲットに到達する最速 ETA。"""
    per = compute_rival_eta_per_player(target, my_player, fleets, planets, angular_velocity)
    return min(per.values()) if per else math.inf
```

`enumerate_candidates` の `compute_rival_eta` 呼び出しに `angular_velocity` を渡す:

```python
rival_eta = compute_rival_eta(t, player, fleets, all_planets, angular_velocity)
```

- [ ] **Step 4: テスト通過確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py -v
```

期待: 全 PASS

- [ ] **Step 5: コミット**

```bash
git add agents/mine/planet_intercept/src/targeting.py agents/mine/planet_intercept/tests/test_targeting.py
git commit -m "fix(targeting): compute_rival_eta uses intercept_pos for orbital targets"
```

---

## Task 6: End-game timing フィルター (新規ギャップ 3)

**概要:** ゲーム残りターン数が `my_eta` より少ない攻撃はどうせ間に合わないためスキップする。`enumerate_candidates` に `remaining_turns` 引数を追加し、間に合わない候補を除外する。

**Files:**
- Modify: `agents/mine/planet_intercept/src/targeting.py`
- Modify: `agents/mine/planet_intercept/src/agent.py`
- Modify: `agents/mine/planet_intercept/src/utils.py` (parse_obs に turn 追加)
- Test: `agents/mine/planet_intercept/tests/test_targeting.py`

- [ ] **Step 1: 失敗テストを書く**

`tests/test_targeting.py` に追加:

```python
class TestEnumerateCandidatesEndgame:
    def test_unreachable_target_excluded(self):
        mine = P(0, 0, 0, 0, ships=50)
        # 距離 60 の惑星: 1 ship -> speed=1.0 -> eta=60 turns
        far_target = P(1, 1, 60, 0, ships=1, prod=1)
        planets = [mine, far_target]
        # remaining_turns=10 なら eta=60 > 10 で除外される
        cands = enumerate_candidates(mine, planets, fleets=[], player=0, remaining_turns=10)
        assert all(c[0].id != far_target.id for c in cands)

    def test_reachable_target_included(self):
        mine = P(0, 0, 0, 0, ships=50)
        near_target = P(1, 1, 5, 0, ships=1, prod=1)
        planets = [mine, near_target]
        # eta < 5 -> remaining_turns=100 なら含まれる
        cands = enumerate_candidates(mine, planets, fleets=[], player=0, remaining_turns=100)
        assert any(c[0].id == near_target.id for c in cands)

    def test_no_remaining_turns_no_filter(self):
        mine = P(0, 0, 0, 0, ships=50)
        far_target = P(1, 1, 60, 0, ships=1, prod=1)
        planets = [mine, far_target]
        # remaining_turns=None -> フィルターなし
        cands = enumerate_candidates(mine, planets, fleets=[], player=0, remaining_turns=None)
        assert any(c[0].id == far_target.id for c in cands)
```

- [ ] **Step 2: 失敗確認**

```bash
cd agents/mine/planet_intercept && uv run pytest tests/test_targeting.py::TestEnumerateCandidatesEndgame -v
```

期待: FAIL

- [ ] **Step 3: enumerate_candidates に remaining_turns フィルターを追加**

`targeting.py` の `enumerate_candidates` シグネチャに `remaining_turns: int | None = None` を追加し、sun チェック後に以下を挿入:

```python
if remaining_turns is not None and my_eta > remaining_turns:
    continue
```

完全なシグネチャ:

```python
def enumerate_candidates(
    my_planet: Planet,
    all_planets,
    fleets,
    player: int,
    top_n: int = 16,
    angular_velocity: float = 0.0,
    planned: dict | None = None,
    mode: str = "neutral",
    remaining_turns: int | None = None,
):
```

- [ ] **Step 4: parse_obs に turn と remaining_turns を追加**

`utils.py` の `parse_obs` を修正:

```python
def parse_obs(obs):
    if isinstance(obs, dict):
        raw_planets = obs.get("planets", [])
        raw_fleets = obs.get("fleets", [])
        player = obs.get("player", 0)
        angular_velocity = obs.get("angular_velocity", 0.0)
        step = obs.get("step", 0)
    else:
        raw_planets = obs.planets
        raw_fleets = obs.fleets
        player = obs.player
        angular_velocity = getattr(obs, "angular_velocity", 0.0)
        step = getattr(obs, "step", 0)
    planets = [Planet(*p) for p in raw_planets]
    fleets = [Fleet(*f) for f in raw_fleets]
    episode_steps = 500
    remaining_turns = episode_steps - step
    return player, planets, fleets, angular_velocity, remaining_turns
```

- [ ] **Step 5: agent.py を remaining_turns 対応に更新**

`agent.py` の `parse_obs` 呼び出し部分を修正:

```python
player, planets, fleets, angular_velocity, remaining_turns = parse_obs(obs)
```

`enumerate_candidates` 呼び出しに `remaining_turns=remaining_turns` を追加。

- [ ] **Step 6: 全テスト通過確認**

```bash
cd agents/mine/planet_intercept && uv run pytest -v
```

期待: 全 PASS

- [ ] **Step 7: コミット**

```bash
git add agents/mine/planet_intercept/src/targeting.py agents/mine/planet_intercept/src/agent.py agents/mine/planet_intercept/src/utils.py agents/mine/planet_intercept/tests/test_targeting.py
git commit -m "feat(targeting): skip targets unreachable before game end"
```

---

## Task 7: Smoke test + TrueSkill 計測チェックポイント

**概要:** 各フィーチャー群の後に smoke test を走らせ、クラッシュゼロを確認してから TrueSkill gauntlet 計測に進む。このタスクは実装ではなく計測手順の定義。

**Files:**
- Read: `runs/trueskill.json` (ベースライン確認)
- Run: gauntlet スクリプト (プロジェクト既存の計測コマンドを使用)

- [ ] **Step 1: Phase 1c 前のベースライン確認**

```bash
cat runs/trueskill.json | python3 -c "import json,sys; d=json.load(sys.stdin); [print(k,round(v['mu'],1)) for k,v in sorted(d.items(), key=lambda x: -x[1]['mu'])]"
```

- [ ] **Step 2: P1+P2 後の smoke test**

Task 1-3 完了後に実行:

```bash
cd agents/mine/planet_intercept && uv run pytest -v
```

クラッシュゼロを確認。

- [ ] **Step 3: P3 後の smoke test**

Task 4 完了後に実行:

```bash
cd agents/mine/planet_intercept && uv run pytest -v
```

- [ ] **Step 4: 全実装後の smoke test**

Task 5-6 完了後に実行:

```bash
cd agents/mine/planet_intercept && uv run pytest -v
```

- [ ] **Step 5: gauntlet 計測**

計測コマンドはプロジェクトの Makefile または scripts/ に従う。TrueSkill mu の前後比較を記録する。

---

## 実装優先度サマリ

```
Phase 1c:
  Task 1-2: P1 In-flight Fleet Tracking    (連射抑制 + garrison 精度)
  Task 3:   P2 Doomed Detection            (退避で艦船ロス削減)
  Task 4:   P3 State Machine               (behind/ahead モード切替)
  Task 5:   新規ギャップ1 Orbital rival ETA修正 (軌道惑星価値評価精度)
  Task 6:   新規ギャップ3 End-game filter   (無駄撃ち削減)

Phase 2 候補 (本計画のスコープ外):
  P4 Snipe Mission
  P5 Crash Exploit
  P6 Multi-source Swarm
  P7 Rear-to-Front Logistics
  新規ギャップ2 Comet 戦略
```
